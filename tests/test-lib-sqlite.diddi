!# TODO: We should improve the SQLite library before trying to
!#       add a corresponding test.

load_module("sqlite");

warning("This test is currently empty, since the SQLite DiddiScript library needs to be improved.");
